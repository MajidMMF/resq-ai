Temporary chat UI for testing the agent service from the frontend.

- Open the frontend app.
- Make sure agent service is running on port 8004.
- Set `VITE_SERVER_URL` to `http://localhost:8004` in the frontend `.env` if needed.
- Temporarily set `VITE_INTERNAL_SECRET` to match the agent service `INTERNAL_SECRET` value.
